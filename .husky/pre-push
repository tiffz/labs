#!/usr/bin/env sh

# Non-blocking reminder before push. CI regenerates the catalog automatically,
# so we avoid interrupting push flow with a second forced commit.
if ! npm run check:shared-catalog >/dev/null 2>&1; then
  echo "Shared catalog is out of date (non-blocking): run 'npm run generate:shared-catalog' when convenient."
fi
