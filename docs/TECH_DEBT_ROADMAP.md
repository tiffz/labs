# Tech debt roadmap

Single prioritized registry for structural debt. Before this doc, debt was scattered across
six docs; each item below names its **canonical detail doc** — this file owns only priority,
entry criteria, and status. Update the status column when an item starts or lands; file new
items here first, then link out.

Review cadence: skim during each `labs-session-retrospective` when touching an affected
subsystem; full re-prioritization quarterly with the [`GUIDANCE_EVALS.md`](GUIDANCE_EVALS.md) run,
and via the standing **regression-pattern review + tech-debt tournament** (see
[`CONTINUOUS_PROCESS_IMPROVEMENT.md`](CONTINUOUS_PROCESS_IMPROVEMENT.md) § Standing regression-pattern review).

## 2026-07 tournament winners (waved plan)

Full report + evidence: [`QUALITY_TOURNAMENT_2026-07.md`](QUALITY_TOURNAMENT_2026-07.md) (57 findings, 55 confirmed). Unifying insight: Labs owns the advisory-ratchet pattern (`check-jscpd-ratchet.mjs`) but applied it to only ~2 attributes — clone it to the rest.

- **Wave 1 (freeze accrual, days):** ✅ **per-file size ratchet** landed (`componentSizeGuardrails.test.ts` + baseline). ✅ 🏆 **full-typecheck ratchet** landed (`scripts/check-tsc-ratchet.mjs`, baseline 0). ✅ **react-hooks ratchet** landed as a **CI-only job** (`scripts/check-react-hooks-ratchet.mjs`, baseline **684** — `refs` 419, `set-state-in-effect` 189 dominate; compiler rules take >3 min so it runs in its own `react-hooks-ratchet` CI job, not presubmit). ✅ **knip `exports:'warn'`→ratchet** landed (`scripts/check-knip-exports-ratchet.mjs`, per-file baseline **67** across 49 files; in presubmit + CI checks). ✅ **exhaustive-deps** promoted `warn`→**`error`** directly (active count already 0 — every real case is a documented inline disable — so no ratchet needed; the existing `lint` gate now blocks new violations). ✅ **complexity/function-length ratchet** landed as an advisory nightly job (`scripts/check-complexity-ratchet.mjs`, baseline **586** = 262 `complexity` + 324 `max-lines-per-function`; report-only, never blocks — the follow-up named in the advisory size-ratchet header, #110). Remaining: coverage/hex ratchets, advisory-perf-budget guardrail (dominant e2e-flake cause).
- **Wave 2 (one source of truth, 1–2 wk):** app/route registry + divergence check (`parallel-registry-drift`, the grand-prize regression class), registry-derived icon/CLS smokes.
- **Wave 3 (single-surface → all-surface, 2–3 wk):** `MergePolicy` across all 6 Drive apps (not just EncoreSong), `usePlaybackGeneration` hook, ✅ **VexFlow font-gate enumerator** landed (`src/shared/notation/vexFlowMusicFontGateGuardrails.test.ts`) — walks every `new Renderer(` surface, asserts each routes through `useVexFlowMusicFontReady`/`ensureVexFlowFontsLoaded`, so no new notation surface can skip the gate; ✅ **all 6 surfaces now wired (6/6, ledger empty)** — the last (`melodiaVexFirstMeasure`) landed 2026-07 (#108) by moving `vexFlowFontExport` to a cycle-free `shared/vexflow/` leaf so `shared/music` can import the gate without a `music↔notation` cycle. Remaining: runtime guard-parity test, data-loss coverage-manifest guardrail.
- **Wave 4 (decompositions held by the Wave-1 ratchet):** VexFlowRenderer 1,115-line effect → pure `renderDrumScore`; StanzaWorkspace/SessionScreen splits; Encore dedup + shared-fork migration.
- **Wave 5 (guidance self-checking):** guidance-eval 90-day gate; AGENT_INVARIANTS `enforcement-kind` tagging; doc-link citation validation.

## Prioritized queue

| #   | Item                                                                                                                                                                                                                                                                                | Canonical detail                                                                                                                         | Entry criteria (start when…)                                                                                          | Status   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | **Stanza-Encore sync migration completion** — finish the dual-read/dual-write cutover so practice resources have one owner                                                                                                                                                          | [ADR 0007-revision](adr/0007-revision-stanza-encore-federated-sync.md)                                                                   | Visual + e2e coverage from the screenshot pipeline overhaul is landed and green for ~2 weeks (protects the migration) | queued   |
| 2   | **Drive stack unification** — migrate Stanza (`useStanzaDriveBackup`, ~805 lines) onto `createLabsPortfolioDriveBackup`; Encore last (most bespoke). Gesture migrated (config in `gesturePortfolioDriveBackupConfig.ts`)                                                            | [`DRIVE_SYNC_DATA_LOSS_PREVENTION.md`](DRIVE_SYNC_DATA_LOSS_PREVENTION.md), skill `labs-drive-backup`                                    | Item 1 merged (sync contract stable)                                                                                  | queued   |
| 3   | **`vite.config.ts` modularization** — split ~1,211 lines into `config/` modules (apps, redirects, plugins, test)                                                                                                                                                                    | — (mechanical; no design doc needed)                                                                                                     | Any session already touching vite.config for another reason, or before the Vite 8 major                               | queued   |
| 4   | **CSS `!important` burn-down** — ratchet `check:css-important` baseline (282) stepwise toward <40 by requiring net-negative on touched files instead of a flat ceiling                                                                                                              | [`CSS_IMPORTANT_AUDIT.md`](CSS_IMPORTANT_AUDIT.md)                                                                                       | Immediately; each CSS-touching PR lowers its files' count                                                             | ongoing  |
| 5   | **Dependency majors** — Vite 8, Vitest 4, Tailwind 4, TypeScript 6, MUI 9, TipTap 3, pdfjs 5, three 0.185, name-generator 2 (phased)                                                                                                                                                | [`DEPENDENCY_UPGRADE_PLAN.md`](DEPENDENCY_UPGRADE_PLAN.md), skill `labs-dependency-upgrade`                                              | —                                                                                                                     | done     |
| 6   | **SessionScreen / LibraryScreen decompositions** — `src/scales/components/SessionScreen.tsx` (~3.1k lines), `src/encore/components/LibraryScreen.tsx` (~2.5k), following the StanzaWorkspace pattern                                                                                | [`COMPONENT_DECOMPOSITION_PATTERN.md`](COMPONENT_DECOMPOSITION_PATTERN.md), skill `labs-component-decomposition`                         | StanzaWorkspace decomposition merged without regressions                                                              | deferred |
| 7   | **Shared mega-file decompositions** — `src/shared/notation/ScoreDisplay.tsx` (~1.6k lines), `src/shared/components/music/DrumAccompaniment.tsx` (~1k), `src/scrapboard/…/speechBubbleLayout.ts`                                                                                     | [`COMPONENT_DECOMPOSITION_PATTERN.md`](COMPONENT_DECOMPOSITION_PATTERN.md)                                                               | Next feature or bugfix session that touches the file (decompose before extending)                                     | queued   |
| 7b  | **App mega-file decompositions** — `src/drums/components/VexFlowRenderer.tsx` (~2.3k lines), `src/encore/components/song/useSongPageMediaHub.tsx` (~2.2k), `PlaylistImportDialog.tsx` (~2k), `practice/PracticeExerciseFocusDialog.tsx` (~1.9k)                                     | [`COMPONENT_DECOMPOSITION_PATTERN.md`](COMPONENT_DECOMPOSITION_PATTERN.md), skill `labs-component-decomposition`                         | Next feature or bugfix session that touches the file (decompose before extending)                                     | queued   |
| 8   | **Audio-cluster cycle burn-down** — shrink the 9-module `audio↔playback↔music↔…` strongly connected component; every removed mutual pair shrinks the allowlist in `sharedModuleCycles.test.ts`                                                                                      | `src/shared/sharedModuleCycles.test.ts` (allowlist is the ledger)                                                                        | Any session refactoring shared audio/playback; remove at least one mutual pair per touch                              | ongoing  |
| 9   | **Encore account menu unification** — Encore rebuilt on shared `LabsAccountMenu` slots (`renderTrigger`, `identitySlot`, `integrationsSlot`, `footer`); `LabsAccountIntegrationCard` + `LabsAccountDisplayNameSection` extracted; guardrail `e2e/smoke/encore-account-menu.spec.ts` | [`SHARED_UI_CONVENTIONS.md`](../src/shared/SHARED_UI_CONVENTIONS.md) § Account menu hierarchy + extension slots                          | —                                                                                                                     | done     |
| 10  | **`appSharedThemes.css` split** — break the shared theme sheet into base tokens + per-family layers so app overrides stop stacking specificity                                                                                                                                      | [`SHARED_UI_CONVENTIONS.md`](../src/shared/SHARED_UI_CONVENTIONS.md) § Theming bridge                                                    | After `check:shared-theme-contract` has run green for ~2 weeks (safety net in place)                                  | queued   |
| 11  | **CRDT / op-log sync engine** — replace snapshot + silent-union with per-field clocks or an operation log so both-edited rows merge without review prompts; includes Stanza→factory migration first                                                                                 | [`DRIVE_SYNC_DATA_LOSS_PREVENTION.md`](DRIVE_SYNC_DATA_LOSS_PREVENTION.md), [ADR 0020](adr/0020-silent-union-sync-row-conflicts-only.md) | needsReview prompts observed more than ~1×/month per active user, or a second concurrent-editor use case ships        | deferred |

## Filed 2026-07-25

| Item                                                                                                                                                                                                                                                   | Canonical detail                                                                 | Entry criteria (start when…)                                                                | Status      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------- |
| **Chart-playback single transport (ADR 0025)** — steps 1–2: route chart drums through the chord session's `AudioContext` (kill the dual-clock), then one `scheduleChartMeasure` + single late gate. Step 0 (bus-teardown leak fix + live diagnostics + bounded-voice guard) landed | [ADR 0025](adr/0025-chart-playback-single-transport.md), `PLAYBACK_ARCHITECTURE_REVIEW_2026-07.md` | A `?debug` audio-diagnostics run pins the residual long-play leak; then build with the invariant guardrails | in-progress |
| **Worker `@cloudflare/workers-types` v4→v5** — bump so the peerOptional matches `wrangler@4.112` (the autodeploy sidesteps it via `npx wrangler`, so it only affects local dev + `typecheck:session-bff`)                                            | `workers/labs-session-bff/package.json`                                           | Any session touching the worker                                                             | queued      |
| **Nightly visual-baseline drift** — 5–6 home/palette/lyrefly/ui/scrapboard baselines diverged (07-18 origin) and were never re-imported from Linux CI; the nightly visual step stays red on it independent of the soak                                | [`VISUAL_REGRESSION_AGENT.md`](VISUAL_REGRESSION_AGENT.md), skill `labs-visual-judge` | Immediately — download the nightly `visual-regression-artifacts`, judge, import from Linux  | queued      |
| **Scales debug tiered-gate swap** — `src/scales/App.tsx` + `SessionScreen.tsx` still read the raw `?debug` flag, so anonymous prod `?debug` can fire the debug-only "complete exercise" controls (mutates only the user's own local progress; no data leak). Left out of #120 because a parallel agent owns Scales. Swap to `isLabsDebugFull()` per [ADR 0026](adr/0026-tiered-debug-access.md) | [ADR 0026](adr/0026-tiered-debug-access.md), `src/shared/debug/labsDebugAccess.ts` | The parallel Scales work lands, or its owner hands the app back | queued |
| **Debug dedup + best-practice UX (ADR 0026 follow-up)** — factor the repeated per-app debug docks into shared primitives (state-dump, danger-zone, button styles) and add the reviewed best practices: a keyboard toggle and `?debug` persistence across in-app navigation. Deferred from #120 to keep that PR a focused security fix; these are additive and the keyboard/persistence UX wants a `labs-ux-journey` sketch first | [ADR 0026](adr/0026-tiered-debug-access.md), `src/shared/components/LabsDebugDock.tsx` | Next debug-tooling session | queued |

## Consciously skipped (decided, not forgotten)

Re-open only if the trigger occurs:

- **PR preview deploys** — solo GitHub-Pages portfolio; dev server + visual pipeline cover review needs. Trigger: a second regular contributor.
- **License compliance checks** — no distribution/compliance exposure. Trigger: commercial use.
- **Commit lint / conventional commits** — squash-merge PR titles carry the history. Trigger: automated changelog need.
- **jscpd duplication gate** — Knip + guardrail tests catch the harmful subset; the nightly advisory clone-count ratchet (`npm run check:jscpd-ratchet`, baseline in [`jscpd-baseline.json`](jscpd-baseline.json)) surfaces growth without blocking. Trigger for a blocking gate: duplication-caused bug class appearing twice.
- **knip-ignore shrink** — low value until the ignore list blocks a real detection.
- **Automated LLM guidance-eval harness** (Cursor SDK) — manual protocol in [`GUIDANCE_EVALS.md`](GUIDANCE_EVALS.md) is sufficient at current cadence. Trigger: quarterly manual runs get skipped twice, or guidance regressions recur.
- **Full-suite blocking visual on PRs** — scoped visual is blocking; full stays advisory. Flip trigger: ~2 weeks of clean nightly visual runs ([`VISUAL_REGRESSION_AGENT.md`](VISUAL_REGRESSION_AGENT.md)).
