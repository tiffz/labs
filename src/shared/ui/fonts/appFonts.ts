/**
 * Self-hosted UI fonts for MUI micro-apps. Import once at the top of each `main.tsx`
 * (before `createRoot`). Roboto + Inter only; Caveat (zines), JetBrains (count/agility),
 * and Cormorant Garamond (melodia) are imported per-app to avoid shipping unused CSS.
 */
import './appFontsCore';
