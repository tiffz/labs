/** Debounced auto-push quiet period (Stanza, Scales, future portfolio apps). */
export const LABS_DRIVE_AUTO_PUSH_DEBOUNCE_MS = 6_000;

/** Minimum ms between consecutive auto-pushes on the same session. */
export const LABS_DRIVE_AUTO_PUSH_MIN_INTERVAL_MS = 4_000;
