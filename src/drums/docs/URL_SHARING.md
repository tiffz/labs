# URL sharing (drums) — stub

**Canonical pattern:** [`docs/URL_STATE_PATTERN.md`](../../../docs/URL_STATE_PATTERN.md) · skill `labs-url-state`.

**Implementation:** `src/drums/hooks/useUrlState.ts` + `src/drums/routes/drumsAppUrl.ts`.

Params: `rhythm`, `bpm`, `time` (defaults when omitted: Maqsum / 120 / 4/4). Defaults omitted from the URL when unchanged. Product overview: [`../README.md`](../README.md).
