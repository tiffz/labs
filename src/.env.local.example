# Copy to src/.env.local (gitignored) and restart `npm run dev`.
# See workers/labs-session-bff/README.md for Google Console + manual QA.

VITE_LABS_SESSION_BFF_URL=https://labs-session-bff.tiffz.workers.dev


# Needed for Encore sharing to work
# Set up in Google Cloud Console -> APIs & Services -> Credentials -> Create credentials -> API key
VITE_GOOGLE_API_KEY=test-api-key