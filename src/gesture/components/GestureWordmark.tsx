/** Site logo lockup — Fraunces text-only wordmark (no logomark). */
export default function GestureWordmark(): React.ReactElement {
  return (
    <h1 className="gesture-wordmark">
      <span className="gesture-wordmark-name">
        <span className="gesture-wordmark-the">The </span>
        Gesture Room
      </span>
    </h1>
  );
}
